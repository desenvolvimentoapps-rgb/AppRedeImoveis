import { RegisterForm } from '@/modules/admin/components/RegisterForm'

export default function RegisterPage() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center p-4 bg-muted/40">
            <RegisterForm />
        </main>
    )
}
